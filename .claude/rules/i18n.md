# Internationalization (i18n)

- **Never hardcode strings** in the UI.
- Define all UI texts in a **separate, central place** and reference them from the components.
- **English only** is supported for now.
- Use the **standard Angular pipes** (e.g. `CurrencyPipe`, `DecimalPipe`, `DatePipe`) for formatting values like currency, amounts, and dates.
